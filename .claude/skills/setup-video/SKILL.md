---
name: setup-video
description: |
  Заполнение информации о конкретном видео (секция 9 NICHE_STYLE_GUIDE).
  Вызывать перед началом работы над каждым новым скриптом.
  Обновляет docs/ref/NICHE_STYLE_GUIDE.md — тему, тезис, обязательные точки.
user-invocable: true
---

# Setup Video — Настройка темы видео

## Назначение

Заполнить или обновить секцию 9 (Тема текущего видео) в `docs/ref/NICHE_STYLE_GUIDE.md`.
Вызывается перед началом работы над каждым новым скриптом.

## Предусловие

Файл `docs/ref/NICHE_STYLE_GUIDE.md` должен существовать с заполненными секциями 1-8 и 10.
Если файл не найден или секции 1-8/10 содержат плейсхолдеры `{{...}}` — направить пользователя на `/setup-channel`.

## Алгоритм

### 1. Проверка

- READ `docs/ref/NICHE_STYLE_GUIDE.md`
- Убедиться, что файл существует.
- Проверить секции 1-8 и 10: не должно быть `{{...}}` плейсхолдеров (кроме секции 9).
- Если предусловие не выполнено — сообщить и предложить `/setup-channel`.

### 2. Интерактивный опрос

Провести пользователя по полям секции 9 через AskUserQuestion.
Все вопросы можно задать одним сообщением — пользователь ответит на все сразу или по частям.

Поля секции 9:
- **Рабочее название видео** — {{VIDEO_TITLE}}
- **Тема** (развёрнутое описание, 2-5 предложений) — {{VIDEO_TOPIC}}
- **Главный тезис / ключевой инсайт** (одно предложение, ради которого зритель досмотрит) — {{KEY_INSIGHT}}
- **Целевая длительность** — {{TARGET_DURATION}}
- **Целевое настроение** — {{TARGET_MOOD}}
- **Обязательные точки контента** (3-7 пунктов, что точно должно быть) — {{MUST_INCLUDE}}
- **Чего НЕ должно быть** (что исключить) — {{MUST_EXCLUDE}}
- **Источники от автора** (ссылки на статьи, исследования, видео; или "будут предоставлены на этапе ресёрча") — {{AUTHOR_SOURCES}}

### 3. Обновление файла

- Заменить ТОЛЬКО секцию 9 в `docs/ref/NICHE_STYLE_GUIDE.md`.
- Не трогать секции 1-8 и 10.
- Использовать Edit tool для точечной замены секции 9.

### 4. Валидация

- Проверить: в секции 9 нет оставшихся `{{...}}` плейсхолдеров.
- Если есть пропущенные поля — запросить у пользователя.

### 5. Пост-setup

Сообщить пользователю:
- Тема видео заполнена в `docs/ref/NICHE_STYLE_GUIDE.md`, секция 9.
- Можно запускать Team Lead промпт для продакшена скрипта.
